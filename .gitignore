# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# scikit-build-core / CMake
_skbuild/
CMakeCache.txt
CMakeFiles/
*.so
*.pyd

# Downloaded benchmark automata (fetched on demand, SHA-256 verified;
# see gpufsm.io.datasets — never commit them, they are large and third-party)
data/anmlzoo/
*.anml

# Measurement output that is not a committed result. cross_arch/ holds re-runs on rented
# GPUs; a file there is committed only when a document cites it.
paper/data/cross_arch/*
!paper/data/cross_arch/dfa_latch_a100.csv
*.tmp.csv

# Editors / OS
.DS_Store
.idea/
.vscode/
